DEFINE QLOCAL('MQ.QUEUE1') DEFPSIST(YES) MAXDEPTH(300) REPLACE
DEFINE QLOCAL('MQ.QUEUE2') DEFPSIST(YES) MAXDEPTH(300) REPLACE

DEFINE QLOCAL('MQ.QUEUE3') DEFPSIST(YES) MAXDEPTH(300) REPLACE
DEFINE QLOCAL('MQ.QUEUE4') DEFPSIST(YES) MAXDEPTH(300) REPLACE
DEFINE QLOCAL('MQ.QUEUE5') DEFPSIST(YES) MAXDEPTH(300) REPLACE
DEFINE QLOCAL('MQ.QUEUE6') DEFPSIST(YES) MAXDEPTH(300) REPLACE

DEFINE QLOCAL('MQ.QUEUE.STREAMINGQ') REPLACE
ALTER QLOCAL('MQ.QUEUE') STRMQOS(BESTEF) STREAMQ('MQ.QUEUE.STREAMINGQ')

DEFINE CHANNEL(TUECHL9) CHLTYPE(SVRCONN) TRPTYPE(TCP) SSLCAUTH(OPTIONAL) SSLCIPH('ANY_TLS12_OR_HIGHER') REPLACE
SET CHLAUTH(TUECHL9) TYPE(BLOCKUSER) USERLIST('nobody') ACTION(ADD)

DEFINE CHANNEL(METRICS) CHLTYPE(SVRCONN) TRPTYPE(TCP) SSLCAUTH(OPTIONAL) REPLACE
SET CHLAUTH(METRICS) TYPE(BLOCKUSER) USERLIST('nobody') ACTION(ADD)
